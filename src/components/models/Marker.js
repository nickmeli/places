export class MarkerObject {

    constructor(lat, lng, icon, title, infoWindowContent, photo, marker) {
        this.lat = lat;
        this.lng = lng;
        this.icon = icon;
        this.title = title;
        this.infoWindowContent = infoWindowContent;
        this.photo = photo;
        this.marker = marker;
    }
}